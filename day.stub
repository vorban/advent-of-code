package solutions

import "core:fmt"

import "../utils"

SOLVER_YYYY_DD :: utils.Solver {
    silver = proc(input: string) -> string {
        return fmt.aprintf("silver")
    },
    gold = proc(input: string) -> string {
        return fmt.aprintf("gold")
    },
}
